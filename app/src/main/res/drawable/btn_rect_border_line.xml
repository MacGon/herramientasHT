<?xml version="1.0" encoding="utf-8"?>
<shape android:shape="rectangle"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- background -->
    <solid android:color="@color/not_available" />        <!-- border -->
    <stroke
        android:color="@color/not_available"
        android:width="3dp" />
    <padding android:left="1dp"
        android:top="1dp"
        android:right="1dp"
        android:bottom="1dp"/>
    <!-- corner roundness -->
    <corners android:radius="20px" />
</shape>